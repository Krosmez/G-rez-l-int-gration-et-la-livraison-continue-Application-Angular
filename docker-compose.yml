services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: olympic-participation-tracker:latest
    container_name: olympic-frontend
    ports:
      - "80:80"
    restart: unless-stopped
